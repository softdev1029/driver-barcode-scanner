<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/bg">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/margin_top_logo"
        android:src="@drawable/logo"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <android.support.constraint.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <Button
            android:id="@+id/button_gateway"
            android:layout_width="@dimen/width_main_btn"
            android:layout_height="@dimen/height_main_btn"
            android:background="@color/btn"
            android:text="@string/btn_gateway"
            android:textColor="@color/white"
            app:layout_constraintBottom_toTopOf="@id/button_driver" />

        <Button
            android:id="@+id/button_driver"
            android:layout_width="@dimen/width_main_btn"
            android:layout_height="@dimen/height_main_btn"
            android:layout_marginTop="@dimen/margin_hor_main_btn"
            android:background="@color/btn"
            android:text="@string/btn_driver"
            android:textColor="@color/white"
            app:layout_constraintTop_toBottomOf="@id/button_gateway" />
    </android.support.constraint.ConstraintLayout>
</android.support.constraint.ConstraintLayout>